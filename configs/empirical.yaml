experiment:
  name: empirical_macro_stress
  mode: empirical
  seed: 42

empirical:
  catalog_path: data/catalog.example.yaml
  output_path: data/processed/catalog_panel.csv

analysis:
  threshold_quantile: 0.95
  run_length: 3
  min_observations: 24

outputs:
  directory: outputs/empirical_macro_stress
