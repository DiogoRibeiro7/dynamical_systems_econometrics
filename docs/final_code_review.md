# Final Code Review

## Executive summary

The repository is in a materially stronger state for reproducible methodological work. Core rare-event, recurrence, multivariate, transform, and orchestration layers now exist as package code rather than only implicit workflow assumptions.

## Pass / fail checklist

- package logic lives in `src/`: pass
- scripts orchestrate without hiding core logic: pass
- config files are used consistently: pass
- figures used by the repo are generated by code: pass
- unsupported causal claims are avoided: pass
- package CLI under `python -m dynsys_econometrics`: fail
- exact prompt-target notebook filenames: fail

## Serious issues

- the empirical experiment still depends on user-supplied canonical CSV data
- notebook execution is not covered by automated tests

## Minor issues

- new plot-returning helpers do not yet have dedicated test files
- optional online-source fetching remains intentionally lightweight

## Reproducibility status

- synthetic experiments are reproducible from configuration
- article figure generation is reproducible from script entry points
- catalog and schema layers are documented

## Claims supported by code

- extreme events can be extracted and clustered reproducibly
- return-time and recurrence summaries can be computed on synthetic and empirical panels
- multivariate joint stress can be described as synchronized exceedances
- baseline econometric diagnostics can be aligned with event outputs

## Claims not yet supported by code

- strong substantive empirical conclusions without a real configured dataset
- causal identification
- deterministic-law claims about observed economies

## Recommended next steps

1. add direct tests for the new plot-returning helpers
2. add a package CLI if `python -m dynsys_econometrics` is desired
3. run an empirical experiment on a curated public dataset and freeze the config
